============
Installation
============

At the command line::

    $ easy_install arch-mirror-archive

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv arch-mirror-archive
    $ pip install arch-mirror-archive
